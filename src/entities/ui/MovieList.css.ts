import { style } from '@vanilla-extract/css'

export const container = style({
  padding: 0,
  margin: 0,
  width: '100%',
  maxWidth: '1200px',
  display: 'flex',
  listStyle: 'none',
  overflow: 'hidden',
  gap: 10,
})
